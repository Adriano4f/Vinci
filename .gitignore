# Dependencies (reinstall with: npm install)
node_modules/

# Build output for static hosts (generated by: npm run build)
dist/

# Note: js/ is committed on purpose — it lets the site work (e.g. on GitHub
# Pages) without anyone needing to run the TypeScript compiler first.
